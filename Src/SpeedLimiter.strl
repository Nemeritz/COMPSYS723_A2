module SpeedLimiter:

input Speed : float;
output SpeedOk;
constant SpeedMin : float;
constant SpeedMax : float; 

loop
    if ?Speed > SpeedMin and ?Speed < SpeedMax then
        emit SpeedOk;
    end if;
each tick;

end module
